const ns = {
  NAME: "UU5.Forms",
  CSS: "uu5-forms",
  name(name) {
    return ns.NAME + "." + name;
  },
  css() {
    return Array.prototype.slice.call(arguments).map(name => ns.CSS + "-" + name).join(" ");
  }
};

export default ns;